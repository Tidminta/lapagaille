#include "../../includes/libshell.h"
int	main(void)
{
	int i;
	char *str;
	char *ptr;
	void *g;

	i = -1;
	while (++i < 10)
	str = ft_strdup("test");
	//str = to_gc(str, ft_strdup("test2"));
	//collect();
	return (0);
}

